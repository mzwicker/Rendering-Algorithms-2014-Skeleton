/**
 * Simple test scenes to compare your renderer to a reference solution.
 * Output images of these scenes are in the "testscenes output" directory. 
 * These scenes cannot be rendered with the base code because they use
 * additional classes that you are supposed to implement!
 */
package rt.testscenes;